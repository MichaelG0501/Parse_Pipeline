####################
# Auto_parse_pseudotime_linear_plot.R
#
# Sample-level Parse trajectory report using samples in place of states.
# Reuses the T0-rooted Monocle3 trajectory generated by
# Auto_parse_pseudotime_samples.R, or rebuilds it if assets are missing.
####################

suppressPackageStartupMessages({
  library(ggridges)
})

helper_candidates <- c(
  "analysis/trajectory/Auto_parse_pseudotime_helpers.R",
  "analysis/cell_states/Auto_parse_pseudotime_helpers.R",
  "Auto_parse_pseudotime_helpers.R"
)
helper_path <- helper_candidates[file.exists(helper_candidates)][1]
if (is.na(helper_path)) stop("Could not find Auto_parse_pseudotime_helpers.R")
source(helper_path)

build_weighted_ridge_df <- function(proj_df, sample_levels, n_points = 512) {
  df <- proj_df %>%
    filter(is.finite(pseudotime), !is.na(sample)) %>%
    mutate(sample = factor(as.character(sample), levels = sample_levels))

  if (nrow(df) == 0) return(NULL)

  counts <- df %>%
    count(sample, name = "n") %>%
    complete(sample = factor(sample_levels, levels = sample_levels), fill = list(n = 0)) %>%
    mutate(
      weight = ifelse(max(n) > 0, n / max(n), 0),
      y = rev(seq_along(sample_levels))
    )

  pt_range <- range(df$pseudotime, na.rm = TRUE)
  if (!all(is.finite(pt_range))) return(NULL)
  if (diff(pt_range) == 0) pt_range <- pt_range + c(-1e-6, 1e-6)
  focus_range <- as.numeric(quantile(df$pseudotime, probs = c(0.01, 0.99), na.rm = TRUE, names = FALSE))
  if (!all(is.finite(focus_range)) || diff(focus_range) <= 0) focus_range <- pt_range

  ridge_df <- bind_rows(lapply(seq_len(nrow(counts)), function(i) {
    sample_i <- counts$sample[i]
    n_i <- counts$n[i]
    weight_i <- counts$weight[i]
    y_i <- counts$y[i]
    sub <- df %>% filter(sample == sample_i)

    if (n_i < 2) {
      x_grid <- seq(pt_range[1], pt_range[2], length.out = n_points)
      dens_y <- rep(0, length(x_grid))
    } else {
      dens <- density(
        sub$pseudotime,
        from = pt_range[1],
        to = pt_range[2],
        n = n_points,
        na.rm = TRUE,
        bw = "nrd0"
      )
      x_grid <- dens$x
      dens_y <- dens$y
    }

    data.frame(
      sample = factor(as.character(sample_i), levels = sample_levels),
      x = x_grid,
      height = dens_y * weight_i,
      y = y_i,
      n_cells = n_i,
      rel_weight = weight_i,
      stringsAsFactors = FALSE
    )
  }))

  max_h <- max(ridge_df$height, na.rm = TRUE)
  if (is.finite(max_h) && max_h > 0) {
    ridge_df <- ridge_df %>% mutate(height = height / max_h * 0.9)
  }

  sample_labels <- counts %>%
    mutate(label = paste0(as.character(sample), " (n=", n, ")")) %>%
    select(sample, y, label)

  list(ridge_df = ridge_df, sample_labels = sample_labels, counts = counts, focus_range = focus_range)
}

message("=== Parse sample-level pseudotime report ===")
result <- get_or_build_parse_trajectory(rebuild = FALSE)
cds <- result$cds
sample_levels <- result$sample_levels
sample_cols <- get_sample_colours(sample_levels)

sample_map <- as.character(colData(cds)$sample_label)
names(sample_map) <- colnames(cds)

graph_bits <- extract_graph_structure(cds)
proj_df <- project_cells_to_graph(cds, graph_bits$edges, sample_map)
root_label_df <- get_root_label_position(cds, graph_bits, result$root_cells)
ridge_bits <- build_weighted_ridge_df(proj_df, sample_levels)

write_csv(proj_df, file.path(pseudotime_dir, "Auto_parse_sample_pseudotime_projections.csv"))

sample_counts <- table(factor(proj_df$sample, levels = sample_levels))
legend_labels <- setNames(
  paste0(sample_levels, " (", as.integer(sample_counts[sample_levels]), ")"),
  sample_levels
)

x_span <- diff(range(c(graph_bits$nodes$x, proj_df$graph_x), na.rm = TRUE))
y_span <- diff(range(c(graph_bits$nodes$y, proj_df$graph_y), na.rm = TRUE))
if (!is.finite(x_span) || x_span == 0) x_span <- 1
if (!is.finite(y_span) || y_span == 0) y_span <- 1
root_n <- sum(as.character(proj_df$sample) == root_sample, na.rm = TRUE)

p_proj_sample <- ggplot() +
  geom_segment(
    data = graph_bits$edges,
    aes(x = x, y = y, xend = xend, yend = yend),
    linewidth = 0.9, color = "grey55", alpha = 0.65
  ) +
  geom_point(
    data = proj_df,
    aes(x = graph_x, y = graph_y, color = sample),
    size = 0.7, alpha = 0.75
  ) +
  {
    if (!is.null(root_label_df))
      geom_point(
        data = root_label_df,
        aes(x = x, y = y),
        shape = 8, size = 4.2, stroke = 1.0, color = "black"
      )
  } +
  {
    if (!is.null(root_label_df))
      geom_label(
        data = root_label_df,
        aes(x = x + 0.03 * x_span, y = y + 0.04 * y_span,
            label = paste0("ROOT\n", root_sample, "\n(n=", root_n, ")")),
        size = 3.2, label.size = 0.25, hjust = 0, vjust = 0,
        fill = "white", color = "black", alpha = 0.95
      )
  } +
  scale_color_manual(values = sample_cols, breaks = sample_levels, labels = legend_labels, drop = FALSE) +
  coord_equal() +
  theme_classic() +
  labs(title = "Principal Graph: sample-projected cells", x = "Dim 1", y = "Dim 2", color = "Sample")

p_proj_pt <- ggplot() +
  geom_segment(
    data = graph_bits$edges,
    aes(x = x, y = y, xend = xend, yend = yend),
    linewidth = 0.9, color = "grey55", alpha = 0.65
  ) +
  geom_point(
    data = proj_df,
    aes(x = graph_x, y = graph_y, color = pseudotime),
    size = 0.7, alpha = 0.75
  ) +
  {
    if (!is.null(root_label_df))
      geom_point(
        data = root_label_df,
        aes(x = x, y = y),
        shape = 8, size = 4.2, stroke = 1.0, color = "black"
      )
  } +
  {
    if (!is.null(root_label_df))
      geom_label(
        data = root_label_df,
        aes(x = x + 0.03 * x_span, y = y + 0.04 * y_span, label = "ROOT"),
        size = 3.2, label.size = 0.25, hjust = 0, vjust = 0,
        fill = "white", color = "black", alpha = 0.95
      )
  } +
  scale_color_viridis_c(option = "D", na.value = "grey85") +
  coord_equal() +
  theme_classic() +
  labs(title = "Principal Graph: pseudotime-projected cells", x = "Dim 1", y = "Dim 2", color = "Pseudotime")

p_umap_pt <- ggplot(
  proj_df %>% filter(is.finite(umap_x), is.finite(umap_y)),
  aes(x = umap_x, y = umap_y, color = pseudotime)
) +
  geom_point(size = 0.65, alpha = 0.75) +
  scale_color_viridis_c(option = "D", na.value = "grey85") +
  coord_equal() +
  theme_classic() +
  labs(title = "UMAP: pseudotime", x = "UMAP 1", y = "UMAP 2", color = "Pseudotime")

if (!is.null(ridge_bits) && nrow(ridge_bits$ridge_df) > 0) {
  p_ridges <- ggplot(
    ridge_bits$ridge_df,
    aes(x = x, y = y, height = height, group = sample, fill = sample)
  ) +
    geom_ridgeline(
      stat = "identity",
      scale = 1,
      alpha = 0.85,
      color = "white",
      linewidth = 0.35
    ) +
    scale_fill_manual(values = sample_cols, drop = FALSE) +
    coord_cartesian(xlim = ridge_bits$focus_range) +
    scale_y_continuous(
      breaks = ridge_bits$sample_labels$y,
      labels = ridge_bits$sample_labels$label,
      expand = expansion(mult = c(0.03, 0.1))
    ) +
    theme_classic() +
    theme(
      legend.position = "none",
      axis.title.y = element_blank(),
      axis.text = element_text(size = 11, colour = "black"),
      axis.title.x = element_text(size = 12, face = "bold"),
      plot.title = element_text(size = 13, face = "bold")
    ) +
    labs(
      title = "Pseudotime density ridges by sample (1st-99th percentile focus)",
      x = "Pseudotime"
    )
} else {
  p_ridges <- ggplot() +
    theme_void() +
    labs(title = "Pseudotime density ridges") +
    annotate("text", x = 0.5, y = 0.5, label = "Insufficient finite pseudotime values", size = 5)
}

report_page <- (p_proj_sample + p_proj_pt) /
  (p_umap_pt + p_ridges) +
  plot_annotation(
    title = "Parse Sample Trajectory Report",
    subtitle = paste0(
      "Total cells: ", nrow(proj_df),
      " | Root sample: ", root_sample,
      " | Root cells: ", root_n
    ),
    theme = theme(
      plot.title = element_text(face = "bold", size = 18),
      plot.subtitle = element_text(size = 11)
    )
  )

pdf_path <- file.path(pseudotime_dir, "Auto_parse_pseudotime_sample_linear_report.pdf")
pdf(pdf_path, width = 16, height = 12, onefile = TRUE)
print(report_page)
dev.off()

message("Saved Parse sample trajectory report to: ", pdf_path)
